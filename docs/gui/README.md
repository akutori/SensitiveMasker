# GUI画面・機能一覧

GUI(Tauri)が提供する画面と機能の一覧。ビルド・起動方法は[README.md](../../README.md#gui)を参照。

## 初回セットアップ

初回起動時のみ表示される。「始める」を押すと、鍵ファイルと暗号化データベースをOS標準のアプリデータフォルダに作成する(CLIの`masker init`と同じ処理)。2回目以降の起動では表示されない。

## メイン画面

起動直後に表示されるマスキング実行画面。

| 操作 | 内容 |
|---|---|
| プロファイル選択 | ドロップダウンでアクティブプロファイルを切り替える(切り替えは即座にDBへ反映される) |
| プロファイル一覧 | プロファイル管理画面を開く |
| インポート | エクスポート済みファイル(`.smx`)からのインポートダイアログを開く |
| 再読み込み | プロファイル一覧・タグ一覧を再取得する(他画面・トレイ・別プロセス(CLI等)での変更を反映する) |
| 新規作成 / プロファイルを編集 | ルール編集画面を開く(プロファイルが1件も無い場合、編集は無効) |
| 入力テキスト | Monaco Editorで編集(Ctrl+Fで検索、Ctrl+Hで置換) |
| ファイルから | 選択したファイルをUTF-8として読み込み、「入力テキスト欄に読み込む」(入力欄に既存内容がある場合は上書き確認)または「直接マスクして別ファイルに保存」を選べる。UTF-8として解釈できないバイト列があった場合は置き換えて読み込みを継続し、警告を表示する(停止はしない) |
| マスク実行 | アクティブプロファイルの全有効ルールを適用し、出力欄に結果を表示する(プロファイル未選択時は無効) |
| クリア | 入力・出力欄と、連番モードの対応表(マッピング)を破棄する |
| 出力テキスト | 読み取り専用。「ファイルに保存」または「クリップボードにコピー」で書き出す |

連番モード(`sequential`)の採番はプロファイルごとに保持され、同じ値は同じダミー値に置き換えられる(「クリア」でリセットするまで、同一セッション内で一貫する)。

「ファイルから」の「直接マスクして別ファイルに保存」は、保存前にルールごとのマッチ件数を確認する画面を経由する(マスク後の内容そのものはこの画面には表示されない)。マッチ件数が0件のルールがある場合は注意書きが表示される。

## プロファイル管理画面

| 操作 | 内容 |
|---|---|
| 検索 | プロファイル名の部分一致で絞り込む |
| 並び替え | 更新日時等での並び替え |
| ★お気に入りのみ | お気に入り登録済みのプロファイルのみ表示する |
| タグ絞り込み | 選択した全てのタグを持つプロファイルのみ表示する(AND条件) |
| + 新規プロファイル / テンプレートから作成 | ルール編集画面を開く(テンプレートは組み込みのルール例からの作成) |
| タグを管理 | タグの作成・改名・削除(プロファイルへの割り当てとは別画面) |
| 全体エクスポート / インポート | 下記「エクスポート/インポート」参照 |
| envインポート | 下記「envインポート」参照 |

各プロファイル行の操作:

- **編集**: ルール編集画面を開く
- **タグ**: そのプロファイルへのタグ割り当てを変更する
- **複製**: 名前を指定して複製する(同名がある場合はエラー)
- **エクスポート**: そのプロファイル単体をエクスポートする
- **削除**: アクティブなプロファイルは削除できない(先に他のプロファイルへ切り替える必要がある)
- **★**: お気に入りの切り替え
- 行のクリック: そのプロファイルをアクティブにする(編集は「編集」から)

ルールが1件も無い、または全ルールが無効化されているプロファイルには警告アイコンが表示される(マスクされないまま気付かず使われることを防ぐため)。

## ルール編集画面

| 操作 | 内容 |
|---|---|
| プロファイル名・説明・タグ | プロファイルのメタ情報 |
| ルール一覧 | ドラッグ&ドロップで並べ替え、チェックボックスで個別に有効/無効を切り替え |
| + ルールを追加 | 種別(リテラル/正規表現)・モード(固定/連番)・パターン等を入力するダイアログを開く。テンプレートからの入力補完も可能 |
| 編集 / 複製 / 削除 | 各ルールに対する操作(同名ルールは作成できない) |
| サンプルテキスト / マスク結果 | 現在編集中の全ルールをリアルタイムに適用したプレビュー(保存前に確認できる) |
| 保存 / キャンセル | キャンセル時、未保存の変更があれば破棄確認ダイアログを表示する |

## エクスポート/インポート

プロファイルをファイル(`.smx`、[age](https://age-encryption.org/)形式で暗号化)として書き出し・取り込みする機能。コマンドの詳細は[masker export](../cli/README.md#masker-export---profile-nameoutput-file) / [masker import](../cli/README.md#masker-import---input-file---yes)と共通(GUIは同じ`profile-store`の処理を呼び出す)。

復号(インポート)の方法は、2つから選べる。**鍵ファイル**(推奨)は、エクスポートのたびに新しい鍵(age x25519)を生成し、拡張子`.smxkey`の鍵ファイルとして、別に保存する。**パスフレーズ**は、GUIが生成するパスフレーズを使う。エクスポートを始めると、まず、この方法を選ぶ画面が開く(鍵ファイルが上に置かれ、初期の選択。縦の幅が小さい画面でも収まるよう、方法の選択と、各方法の画面を、2段に分ける)。インポートでは、ファイルの先頭にある(平文の)ヘッダーから、方法を自動で判別し、方法ごとの入力画面(パスフレーズ入力・鍵ファイルの入力)を開く。

**エクスポート(パスフレーズ方式)**: パスフレーズはGUI側で自動生成される(CLIのような手入力ではない)。「表示」(目のアイコン)で内容確認、「コピー」でクリップボードへコピーできる。コピーの30秒後に自動クリアを試みる(通知と注意文にも、この秒数を表示する)が、Windows以外の環境ではクリップボード履歴に残る場合がある(確実に消すには手動でクリアする必要がある)。コピーの書き込みに失敗した場合も、書き込みの途中で値が残らないよう、同じ値が残っていればクリアを試みる。

エクスポートに成功しても画面は閉じず、パスフレーズを表示(既定は伏せ字)・コピーできる状態のまま、「閉じる」を待つ。この画面を閉じるとパスフレーズは破棄され、二度と表示できない。成功後は「再生成」と「エクスポート」を出さず(書き出したファイルと画面のパスフレーズが食い違うため)、Escapeや背景を押す操作では閉じない(「閉じる」と×だけで閉じる)。保存先を選んでいる間は、再生成とエクスポートを無効にするだけで、閉じることはできる(閉じると、書き出しは取り消される)。書き込み中(保存先が決まってから、書き込みが終わるまで)は、ボタンを全て無効にし、閉じる操作(×・Escape・背景を押す)も受け付けず、フォーカスは画面の内側に留める。書き込み中・成功後は、履歴の移動(マウスの戻るボタンなど)でこの画面ごと消えることも止める。保存先の選択を取り消した場合と書き出しに失敗した場合は、編集中の画面へ戻る。コピー/クリアの処理中は、コピーと再生成を無効にする。エクスポートの完了は、画面の通知に加えて、スクリーンリーダー向けの読み上げの領域にも入る(実機のスクリーンリーダーでの読み上げは未確認)。

<details>
<summary>設計判断の記録(裁量を委任された項目)</summary>

- 委任: 2026-09-19、ユーザーが「UXと安全性を両立した仕様にする」ことを条件に、エクスポート画面・インポート画面の細部の決定をAIへ委ねた。
- 委任: 2026-09-20、ユーザーが「すべてのissueを片付ける」ことを指示し、各Issueの「決めたいこと」(保存先の選択中の扱い・トレイへの格納時の扱い・右クリックのメニュー・鍵ファイル方式など)の決定をAIへ委ねた。
- `evidence_type: delegated`(AIによる一度きりの判断)

| 判断 | 理由 |
|---|---|
| 成功した時点で、表示を伏せ字へ戻す | 表示したまま、画面共有や録画に残さないため |
| 成功後は「再生成」「エクスポート」を出さず、「キャンセル」を「閉じる」にする | 再生成できると、書き出し済みのファイルと画面のパスフレーズが食い違うため |
| 保存先を選んでいる間(「保存先を選択しています…」と示す)は、再生成・エクスポートだけを無効にし、閉じる操作(×・Escape・背景・キャンセル)は受け付ける。閉じると書き出しは取り消され、その後に保存先が決まっても、書き出さない | まだ何も書き出していないので、パスフレーズを失っても、失うものが無く、閉じる操作を止める理由が無いため。Windows 11(Home 10.0.26200)・WebView2 153の実機(2026-09-20。E2Eビルド・tauri-plugin-dialog 2.7.3[rfd 0.16.0])では、本物の保存ダイアログを開いている間、アプリのウィンドウは無効(`IsWindowEnabled`が偽。入力を受けない)で、ダイアログの所有者は、アプリのウィンドウであり、ダイアログはアプリのウィンドウより前面にあり(アプリのウィンドウを最前面へ寄せても、前面のまま)、ダイアログを閉じると、アプリのウィンドウは有効へ戻った。そのため、Windowsでは、ダイアログが画面の裏に隠れることは無く、ダイアログを閉じるまで、画面を閉じる操作も届かない。ダイアログがアプリのウィンドウを無効にしない環境(rfd 0.16.0が、ダイアログの親を使わないLinux[GTK3]。実機は未確認。macOSは、親のウィンドウのシートとして開く。実機は未確認)で、ダイアログが、アプリのウィンドウの裏に隠れる・応答しないときに、画面から抜け出せなくなるのを防ぐ。書き出すパスフレーズを変えさせないため、再生成・エクスポートは無効にする(選択の結果が届いたときに、画面が閉じられていたら、閉じた画面のパスフレーズを、誰も見ないまま書き出さないため、書き込まない) |
| 書き込み中(保存先が決まってから、書き込みが終わるまで。「書き込んでいます…」と示す)は、全操作を無効にし、閉じる操作(×・Escape・背景を押す)も受け付けない | 書き込み中に閉じると、パスフレーズが失われたまま成功の通知だけが出るため |
| 成功後は、Escapeと背景を押す操作では閉じず、「閉じる」と×だけで閉じる | 唯一の表示を、誤操作で失わないため |
| 書き込み中・成功後は、履歴の移動(戻る操作)も止める(保存先を選んでいる間は、止めない) | この画面ごと消えて、パスフレーズを失うため。保存先を選んでいる間は、まだ何も書き出していないため |
| 書き込み中・書き出し済みの間は、右クリックのメニュー(メニューキー・Shift+F10を含む。contextmenuの既定の動作)を出さない。編集中・保存先の選択中と、閉じた後は、出す | WebView2の既定のメニューは有効(wryの既定値)で、「最新の情報に更新」(ページの再読み込み)を含み、再読み込みされると、画面が消えて、パスフレーズを失うため(履歴の移動を止めるのと同じ理由)。実機(2026-09-20、Windows 11・WebView2 153のE2Eビルド)で、右クリックのメニューに「最新の情報に更新」があること、書き出し済みのエクスポート画面が開いている間は、右クリックしてもメニューが出ないことを、スクリーンショットで確かめた(配布ビルドでも、既定のメニューが有効なことは、wryのソースで確かめたが、配布ビルドでの実機の確認は、していない)。実機で確かめたのは、書き出し済みの画面が開いている間と、閉じた後だけで、書き込み中・編集中・保存先の選択中の、ネイティブのメニューと、メニューキー・Shift+F10で開くメニューは、実機では確かめていない(E2Eは、合成した`contextmenu`イベントの既定の動作が止まることを確かめる)。ページ側でイベントを止めるので、WebView2以外にも効くと見込まれるが、macOS(WKWebView)・Linux(WebKitGTK)では、確かめていない。この間は、入力欄の標準のメニュー(コピーなど)も出ないが、パスフレーズの入力欄は読み取り専用で、コピーのボタンがある。アプリ全体で既定のメニューを無効にすると、他の画面の入力欄の貼り付けまで使えなくなるため、この間だけにする。抑止は、描画の状態(書き込み中・書き出し済み)から判定するため、書き込みへ遷移した直後の、1回の再描画までは、効かない(履歴の移動を止めるのと同じ)。再読み込みされると、コピー済みのパスフレーズの自動クリアのタイマー(画面側)も失われる(この変更は、再読み込みの入口を、この間だけ塞ぐもので、タイマーが失われた後の扱いは、変えない) |
| エクスポートを始めると、まず、復号の方法(鍵ファイル・パスフレーズ)を選ぶ画面を開く。鍵ファイルが上に置かれ、初期の選択 | 縦の幅が小さい画面(ノートパソコンなど)に収めるため、方法の選択と、各方法の画面を、2段に分ける。鍵ファイルは、パスフレーズの転記・クリップボードへの残りに起因する問題を持たないため、推奨として、上に置く |
| 鍵ファイル方式は、鍵ファイルの保存先、エクスポートするファイルの保存先の順に選び、両方決まってから、2つのファイルを書く。どちらかを取り消すと、何も書かない。2つのファイルは、保存先と同じフォルダの、推測できない一時の名前(乱数を含む。既に有る名前へは書かない)へ、完全に書き(保管先へ確定させ)、両方が書けた後に、本来の名前へ置き換える。置き換えは、既に有るファイルを、いったん別の名前へ退避してから行い、どちらかの置き換えに失敗したら、置いたファイルを外して、退避したファイルを戻す(戻せなかったときは、退避したファイルを消さず、その場所を、エラーで伝える)。書き込みの前に、置き換えられない保存先(フォルダ・読み取り専用のファイル)を拒否する | 鍵ファイルだけ・エクスポートしたファイルだけが残ると、復号できない・使えないファイルが残るため。置き換えを選んだ、前のエクスポートの鍵ファイルは、途中の失敗で、壊さない(壊すと、前のエクスポートしたファイルを、別の名前で残していても、二度と復号できなくなる。鍵ファイルだけを、先に置き換えると、エクスポートしたファイルの置き換えの失敗で、この状態になる)。退避は、その名前のファイルを、他のプロセスが開いていて置き換えられない場合(Windowsの共有違反など)を、何も変える前に見つけるためでもある。一時の名前が推測できると、保存先のフォルダに書ける別のプロセスが、先に、その名前で、他のファイルへのリンクを置き、秘密が、そのファイルへ書かれうるため、乱数を含め、新しいファイルとして作る |
| 鍵ファイル方式のエクスポートの、既定のファイル名は、日時を含む(例: `sensitivemasker_export_20260920-210509.smx`・`.smxkey`。2つの名前は、そろえる)。プロファイル名は、含めない | 固定の名前だと、2回目のエクスポートで、前の鍵ファイルを、置き換えてしまいやすいため。プロファイル名は、ファイル名(最近使ったファイルの履歴)に、平文のメタデータとして残るため |
| 鍵は、Rustが生成・保存・読み込みを行い、画面(JavaScript)へは、渡さない | パスフレーズと違い、画面へ渡す必要が無く、渡さなければ、JavaScriptの文字列として、消去できないまま、メモリに残らないため |
| 鍵ファイルは、所有ユーザーだけの権限で書く。空のファイルを作って権限を制限してから、内容を書く。Windowsのicaclsは、継承の権限を除く前に、ユーザーへ与える。Unixは、chmodの後に、権限を読み戻して確かめる。権限を制限できない保管先(FAT/exFATのUSBメモリなど)では、書き込みは成功として、その旨を通知する | 鍵ファイルの保管先は、利用者が選ぶ。持ち出し用のUSBメモリなど、権限を持たないファイルシステムも、ありうるため、書き込み自体は、失敗にしない。書いた後に制限すると、その間、秘密が、他のユーザーに読める権限で置かれるため、内容を書く前に制限する。継承の権限を先に除くと、与える処理が失敗したとき(ユーザー名を解決できないなど)、誰も読めない状態になり、「自分だけが読める権限にできなかった」という通知と、実態が食い違うため、与えてから除く(途中で失敗しても、継承の権限が残る)。権限を持たないvfatなどは、chmodが成功しても効かないため、読み戻して確かめる |
| 鍵ファイル方式のエクスポート画面は、書き込み中だけ、閉じる操作を受け付けない(保存先を選ぶ間は、閉じられる)。書き込みが終わると、画面は、閉じる | 保存先を選ぶ間は、まだ何も書き出していないため。書き込み中に閉じても、書き込みは続くため、結果を見失わないよう、閉じさせない。画面に見せる鍵(パスフレーズのような)が無いため、書き出し済みの状態を、残さない |
| インポートは、ファイルの先頭のヘッダーから、方式を自動で判別する(方式を選ぶ画面を設けない) | ファイル自身が、方式を示すため(ageのヘッダーは、平文で、鍵を必要としない)。利用者に、ファイルを見れば分かることを、選ばせないため |
| 判別・復号は、ファイルの先頭のヘッダー(上限2048バイト。正規のヘッダーは、パスフレーズ方式が約150バイト、鍵ファイル方式が約180〜360バイトで、鍵ファイル方式は、ageが足すダミーの受け手の長さで、毎回変わる)だけを解析し、上限を超えるヘッダーは、ageのファイルとして扱わない。判別は、ファイルの先頭だけを読み、全体を読まない | ageのヘッダーの解析は、ヘッダーの長さの二乗の時間を要し(1バイトずつ読み足しては、最初から解析し直す)、細工された巨大なヘッダーで、長時間ハングするため(64KiBで数秒[リリースビルド]、取り込みの上限の8MiBでは何時間) |
| インポートの規模(プロファイル数200・1プロファイルあたりのルール数500・全体のルール数2000)の検査は、型付きの読み取り(ルールごとに、正規表現をコンパイルする)より前に、ルールの中身を読み飛ばして、数える。数える読み取りは、型付きの読み取りと同じ種別・同じキー・同じ形(マップだけ。欄の順に並べた配列の形は、どちらも受け付けない)を読み、読み取れない形は、検査を飛ばさずに、拒否する | 2つの読み取りが、受け付ける形で食い違うと、検査だけが読み取りに失敗して(余分なキーの型が違うだけで)飛ばされ、上限を超えるルールが、全て、型付きの読み取りへ進んで、ルールの数に比例して、正規表現のコンパイルの時間と、メモリを使わされるため |
| エクスポート・テキストの保存先は、親のフォルダが、アプリのデータフォルダの外でも、鍵ファイル・DBファイルの実体そのもの(ハードリンク・シンボリックリンク)を指すなら、拒否する。名前でなく、実体の同一性で比べる(CLIの`--output`も同じ) | 書き込むと、暗号化DB・鍵ファイルを上書きし、全てのプロファイルを復号できなくするため |
| 鍵ファイルは、拡張子`.smxkey`を要求する。ドロップは、ちょうど1つで、拡張子が`.smxkey`のときだけ受け付ける | 複数のファイルから、どれが鍵ファイルかを、推測しないため。エクスポートしたファイル(`.smx`)と、取り違えないため |
| CLIは、鍵ファイル方式を扱わない。鍵ファイルで暗号化されたファイルを、パスフレーズで取り込もうとすると、鍵ファイルが必要な旨のエラーを出す | Issueの範囲外(GUIが落ち着いてから、別途検討する)。「パスフレーズが誤っている」と、誤って案内しないため |
| 閉じる前の確認ダイアログを設けない | 失っても再エクスポートで復旧でき、確認の手間に見合わないため |
| 「表示」の自動再マスクを設けない | 手入力の最中に隠れる不便が、得られる安全性を上回るため |
| コピーの通知と注意文に、自動クリアまでの秒数を表示する | 貼り付ける前にクリップボードが空になる理由と猶予を、利用者が把握できるようにするため |
| インポートの入力欄に「表示」の切り替えを設け、開くたびに伏せ字へ戻す | 内容を確かめられるようにしつつ、録画・配信に映り続けないようにするため |
| インポートの入力欄は、表示にしても自動補完・綴り確認の対象にしない。WebView2の自動補完は、ウィンドウの設定(`generalAutofillEnabled: false`)でも無効にする | 表示中のパスフレーズが、ブラウザ機能の履歴や校正に渡らないようにするため。WebView2は入力欄の`autocomplete="off"`を守らない場合があるため、ウィンドウの設定でも止める(パスワード・クレジットカードの自動補完は対象外。macOS/Linuxでは無視される) |
| 完了の通知の領域を、書き出す前から画面の中に置き(見えず、中身は空)、完了したときに中身を入れる | 領域ごと後から現れると、スクリーンリーダーに読み上げられないことがあるため |
| コピーの書き込みに失敗したときも、同じ値が残っていればクリアを試みる(結果は通知しない) | Windowsでは、書き込みの後に履歴・クラウド同期からの除外を設定する2段階の処理で、後段だけが失敗しても、値がクリップボードに残ることがあるため(失敗は既に通知している) |
| クリップボードへコピーしたパスフレーズの、Rust側の複製(終了時のクリアのために追跡している値・IPCで受け取った引数・クリアの確認で読み取った内容)は、メモリを消去する型(`Zeroizing`)で持ち、使い終えたときに消去する。終了時のクリアでは、追跡している値を取り出して使い(複製を作らない)、非Windowsでは、クリップボードのプラグインへ、借用で渡す(複製を作らない) | パスフレーズが、Rust側のメモリに、消去されずに残り続けないようにするため(機微データは、drop時に消去する、という方針)。消せるのは、Rust側で保持している分だけで、画面のJavaScriptの文字列・TauriのIPC内部の複製・非Windowsでクリップボードのプラグイン(内部のarboard)が作る複製は、アプリから消去できない。トレイの「クリップボードをマスク」(`tray.rs`の`mask_clipboard`)が、クリップボードから読み取る文字列は、消去しない`String`で持つ(パスフレーズがクリップボードに残っている間に実行すると、それを含む。未対応) |
| 保留している復号済みの内容(`ImportPreview`)は、確定・破棄・保持数の上限による廃棄・ページの読み込みのどの経路で捨てても、drop時に、メモリ上で消去する(`ImportPreview`が`Drop`で、ルールのパターン・固定値・名前・タグを消去する。確定は、内容を借用で使う)。アプリの終了では、Tauriが管理している状態をdropせずにプロセスを終えるため、終了の通知(`RunEvent::Exit`)で、保留を全て破棄する(破棄の中でdropされ、消去される)。復号したペイロードのプロファイル(`ExportedProfile`)も、`Drop`で消去する(名前の重複などのエラーで、確認画面へ進まずに捨てる場合も含む)。取り込み(復号・確認・確定)・保存・エクスポートの経路で、アプリが作る複製(復号した平文・JSON・ルールの複製・復号の一時バッファ)も、消去する型に置く: ルールの直列化は、借用のビューで行い(`serde(into)`の全体の複製を作らない)、出力は、先に長さを数えて、消去する型の、事前に確保した領域へ、1回で書く(伸長で、消去されない旧バッファを残さない)。復号は、暗号文と同じ大きさを先に確保した、消去する型へ読み、規模の検査は、ルールの中身を読み飛ばす(JSONの木へ読まない)。復号した平文の読み取りは、種別だけを先に読み、その種別の型へ、そのまま読む(serdeの内部タグ付きenum・flattenは、エスケープが要る文字[`"`・`\`・制御文字]を含む値を、内部のバッファへ複製して、消去されずに残すため、使わない)。プロファイル・ルールの検証で拒否したときも、受け取った内容(ルールのパターン・固定値・接頭辞・説明、プロファイルの名前・説明)を消去してから捨てる(拒否の理由の文章には、ルールの名前と、正規表現として不正なパターンが、利用者へ示すために含まれ、それは消去されない)。プロファイル一覧のルール数の集計は、復号した平文から、ルールの数と有効かどうかだけを読み、ルールの中身は読み飛ばす(JSONの木へ読まない)。テスト(`wipe_check::MarkerScan`)は、目印を入れたリテラルのルールを、これらの経路に通し、目印を含んだまま解放された実体が無いことを確かめる。消せないもの: ageの暗号化が内部に持つ平文の1チャンク(最大64KiB。age 0.12.1の`StreamWriter`)、ageが、復号した1チャンクを、`Vec::into_boxed_slice`で縮めるとき、アロケータが領域を移すと、移る前の領域に、平文が残ること(アロケータ次第。Windowsで観測)、serde_jsonが、エスケープが要る文字を含む値を読むときの作業用のバッファ(1回の読み取りにつき1件)、正規表現のルールについて、`regex`が内部に持つパターンの写し(`Rule::new`の検証・マスクの実行) | ルールのパターン・固定値には、マスク対象の実際の値(envインポートは、値そのものをリテラル一致のルールにする)が入りうるため、確認する人が居なくなった後に、メモリへ残さないようにするため。消せるのはRust側の保留の分だけで、確認画面へ渡すためにIPCで複製した内容(画面のJavaScriptの文字列・TauriのIPC内部の複製)は、アプリから消去できない |
| インポートの復号の間は、OKを押せず、入力も変えられず、その理由を「復号しています…」と示す。閉じることはできる | 復号が重なると、同じ復号が並行して走り、負荷が無駄に増えるため。理由を示さないと、OKが無効になった理由も、開き直した画面が待たされる理由も、分からないため。閉じられなくすると、誤ったファイルを選んだときに、待つしかなくなるため |
| 復号している間に画面が閉じられたら(同じファイルを開き直した場合も含む)、復号が終わっても確認画面へ進まず、届いた結果の保留(復号済みの内容)だけを、その識別子を指定して破棄する。破棄が終わるまで、この画面の次の復号を始めさせない | 確認画面へ進む画面がもう無いのに、復号済みの内容(平文)が、Rust側に残り続けないようにするため。開き直した画面へ古い画面の結果を出すと、何も入力していない画面が確認画面へ進むため。破棄が終わるまで待つのは、画面が操作できる状態へ戻った時点で、古い復号済みの内容が、Rust側に残っていない状態にするため |
| 復号の結果が届き、確認画面へ進むとき、この画面が既に所有している保留(確認画面の描画より前に画面が閉じられて、確認されないまま残ったもの)があれば、新しい結果の保留を所有として記録したうえで、その前の保留の破棄を発行する(破棄の完了は待たない) | 所有する識別子を上書きすると、その保留は誰にも破棄されなくなり、保持数の上限による押し出し・ページの読み込みの開始・アプリの終了まで、復号済みの内容(平文)がRust側に残るため。破棄の完了を待つと、その間に画面が閉じられうるため、新しい保留の所有と確認画面へ進む操作は、同期的に続ける |
| ウィンドウをトレイへ格納する(×ボタン)ときは、Rust側で、保留しているインポートの復号済みの内容を全て破棄し(あわせて、保留の世代を進め)、開いている画面へ知らせる。画面は、パスフレーズ・復号済みの内容・.envの値を持つ画面(インポートのパスフレーズ入力・鍵ファイルの入力・確認、envインポートの選択・名前、編集中・保存先の選択中のエクスポート)を、利用者が取り消したのと同じ扱いで閉じる(閉じる画面の写しは、再描画より先に、閉じた状態へ進める) | 格納しても、WebViewは動き続けるため、そのままにされると、格納している間(何時間でも)、パスフレーズ・復号済みの内容・.envの値が、画面の状態とRust側の保留に残るため。JSの文字列はメモリ上で消去できないため、閉じて消えるのは参照だけである。画面へも知らせるのは、破棄された保留を、確認画面が確定しようとしないよう、画面の状態を、Rust側の保留と一致させるため。世代を進めるのは、格納の前に始めた復号が、格納している間に終わっても、その結果を保留しないため(画面側が、閉じた後に届いた結果を捨てる処理は、格納している間、WebViewの処理が止まっていても、Rust側に保留を残さない、という保証にならない)。鍵ファイルの入力も閉じるのは、復号している最中に格納されると、その結果(復号済みの内容)が、格納している間に、確認画面として届くため。閉じる画面の写しを先に進めるのは、再描画より前に届いた復号の結果を、閉じた画面が、まだ開いているとは見ないため |
| 書き込み中・書き出し済みのエクスポート画面は、トレイへ格納しても閉じず、パスフレーズの表示を伏せ字へ戻す | 書き込み中に閉じると、パスフレーズを失ったまま、ファイルだけが書き出されるため。書き出し済みの画面を閉じると、書き出したファイルを復号するためのパスフレーズを、二度と表示できないため(閉じて失う不便が、閉じずに残すリスクより大きいと判断した)。表示していたパスフレーズが、画面共有・録画に残らないよう、伏せ字へ戻す(書き込みが止まったまま長く格納されても、平文で残さない) |
| メイン画面のファイル取り込みの確認(ファイルの本文・マスク結果の件数)は、トレイへ格納しても閉じない | 編集欄が持つ本文と同じ内容しか持たず、編集欄の本文は、格納しても残るため |
| 知らない種類の画面は、トレイへ格納するときに閉じる | 新しく足した画面が、パスフレーズなどを持っていても、格納したまま残らないようにするため(閉じる側を既定にする) |
| 復号の結果の保留の識別子(0以上の安全な整数)は、フロントエンドが実行時に検証し、無効なら、復号の失敗として扱う。確定・破棄も、無効な識別子では、Rustを呼ばずに拒否する | Rustの破棄は、識別子が無い・nullのとき、全ての保留(他の画面が始めた復号の保留も)を破棄する。応答の形が変わる・識別子がNaNになるなど、取り決めがずれたときに、他の画面の保留が無言で消えないようにするため |
| 画面を離れる(プロファイル管理画面とメイン画面の行き来など)と、その画面が所有する保留(復号の結果が届いた復号済みの内容)を破棄する(確認画面を開いていなくても、無条件に破棄する。所有する保留が無ければ、何も起きない)。確定を始めていれば、破棄しない | 確認する人が居なくなった、復号済みの内容(平文)が、Rust側に残り続けないようにするため。確定を始めた後に破棄すると、その確定が失敗するため。他の画面が始めた復号の保留は、この画面を離れても消えない |
| 復号済みの内容(Rust側の保留)は、復号のたびに払い出す識別子で結び付け、確定・破棄は、その識別子の保留だけに作用する。同時に保持する保留は4件までとし、超えたら最も古いものを捨てる(捨てられた保留の確定は、「確認待ちのインポートがありません」で失敗する)。識別子を省略した破棄は、全ての保留を破棄する(E2Eの後片付け用。画面は使わない) | 復号は、画面ごとに独立して走るため、識別子で結び付けないと、古い画面の復号の結果が新しい画面の保留を上書きしたり、古い画面の破棄が新しい画面の保留を消したり、確認画面に出した内容と確定される内容が食い違ったりするため。上限が無いと、確認されないままの復号済みの内容(平文)が、無制限に溜まりうるため。上限の4件は、実運用で同時に保持しうる数(確認画面に進んだ1件と、遅れて届いて破棄を待つ結果)より十分大きい値として置いた |
| メインウィンドウのページの読み込みが始まったとき(WebViewの再読み込みなど)は、Rust側の保留を全て破棄する。復号している最中にページが読み込み直された場合は、その復号の結果も保留せず、失敗にする。ページ内の画面遷移(履歴による切り替え)は、ページの読み込みを起こさないため、この破棄の対象にならない(WebView2では、そうなる。他のウェブビューでも同じとは限らない) | ページを読み込み直すと、画面のJavaScriptの状態(保留の識別子)が失われ、その保留を確定も破棄もできる画面が無くなる。破棄しないと、確認する人が居ない復号済みの内容(平文)が、保持数の上限による押し出しか、アプリの終了まで、Rust側に残り続けるため |
| インポート・envインポート・「ファイルから」は、ファイルの選択・読み込みを待つ間に別の画面が開かれていたら、その画面を置き換えない | 書き出し中・書き出し済みのパスフレーズや、確認待ちの復号済みの内容を、失わないため |
| インポートで、前後の空白・ゼロ幅文字・BOMを除いて復号できたときは、確認画面で、その旨を知らせる(「パスフレーズの前後にあった空白・不可視文字を取り除いて、復号しました。」)。入力の欄では、除く文字があることを、示さない。コマンドラインの`masker import`は、標準エラー出力に、同じ趣旨を出す | 除いたことを知らせないと、利用者は、意図した空白を含むパスフレーズが、貼り付けで変わっていたことに、気付けないため。実際に除いたかどうかは、Rustが、復号の結果として知っている(復号できたとき、入力どおりでなく、除いた値だったこと)。入力の欄で、JavaScriptの`trim()`で示すと、ゼロ幅文字などを検出できず、Rustが除く文字と食い違うため |
| インポートで、入力どおりで復号できないときだけ、前後の空白・ゼロ幅文字・BOMを除いて再試行する | チャットなどからの貼り付けで混ざりやすい文字で、正しいパスフレーズが弾かれないようにするため。入力どおりで復号できるときは変えず、意図して空白を含むパスフレーズを壊さないため |

</details>

**エクスポート(鍵ファイル方式)**: 「鍵ファイル」を選んで進むと、鍵ファイルを別に保存する旨と、「この鍵ファイルを紛失すると、エクスポートしたファイルは二度と復号できません。」の注意が出る。「エクスポート」を押すと、鍵ファイルの保存先、エクスポートするファイルの保存先の順に選び、両方決まってから、Rustが、鍵を生成して、2つのファイルを書く。どちらかの選択を取り消すと、何も書かない。保存先を選んでいる間は、画面を閉じられる(閉じると、書き出しは取り消される)。書き込み中は閉じられず、終わると、画面は閉じる(鍵は、画面へ渡さないため、画面に見せるものが無い)。鍵ファイルは、所有ユーザーだけの権限で書く(FAT/exFATのUSBメモリなど、権限を持たない保管先では、制限できず、その旨を通知する)。2つのファイルは、一時の名前へ書いてから、両方が書けた後に、本来の名前へ置き換えるため、片方の書き込みに失敗しても、保存先に既にあったファイル(置き換えを選んだ、前のエクスポートの鍵ファイルなど)は、壊れない。既定のファイル名は、日時を含む。鍵ファイルの中身は、ageの鍵ファイルと同じ形式(コメント行と、`AGE-SECRET-KEY-1…`の1行)。

**インポート(鍵ファイル方式)**: 鍵ファイルで暗号化されたファイルを選ぶと、鍵ファイルの入力画面が開く。鍵ファイル(`.smxkey`)は、「鍵ファイルを選択…」か、ウィンドウへのドロップ(1つだけ)で指定する。鍵ファイルの中身は、先頭のBOM・空行・`#`で始まるコメント行・Windowsの改行があっても、読める。指定してOKを押すと、復号して、内容の確認画面へ進む(以降は、パスフレーズ方式と同じ)。復号している間は、ドロップで、鍵ファイルを選び直せない。別のエクスポートの鍵ファイル・拡張子が違うファイル・大きすぎるファイル・エクスポートしたファイルではないファイルなどは、理由を示す(ファイルの方式を判別できない場合は、入力画面を開かず、通知する)。コマンドラインの`masker import`は、鍵ファイル方式のファイルを取り込めず、その旨のエラーを出す。

**インポート**: ファイル選択・パスフレーズ入力(鍵ファイル方式のファイルは、鍵ファイルの入力)の後、取り込まれるプロファイル・ルールの内容(名前・種別・モード・パターン・有効/無効)を一覧表示する確認画面を経てから実行する。全体エクスポートのファイルを取り込む場合、名前が重複するプロファイルは自動的にリネームされる(例: `SIP監視用` → `SIP監視用 (インポート)`)。

パスフレーズの入力欄は伏せ字から始まり、「表示」(目のアイコン)で入力内容を確認できる(手入力の打ち間違いを目で確かめるため)。マウスで押した後も続けて入力でき、画面を開き直すたびに伏せ字へ戻る。

復号(数百ミリ秒〜数秒)の間は、OKを押せず、入力も変えられず、その理由を「復号しています…」と示す(フォーカスは入力欄に置く。閉じることはできる)。復号している間に閉じた場合は、同じファイルを開き直した場合も含め、復号が終わっても確認画面へ進まず、復号済みの内容は破棄される。開き直した画面は、古い復号が終わるまで、入力できない状態で待つ。画面を離れた場合も、復号済みの内容は破棄される。

復号済みの内容(Rust側の保留)は、復号のたびに払い出す識別子で結び付ける。確認画面の確定・取り消し・離脱は、その確認画面を持つ画面(メイン画面・プロファイル管理画面)が所有する識別子の保留だけを対象にし、他の画面が始めた復号の保留には作用しない。そのため、復号している最中に画面を離れ、別の画面で別のファイルの復号を始めても(古い復号の結果が、後から届いても)、新しい画面の確認画面に出た内容と、確定される内容は一致する。保持できる保留は同時に4件までで、超えると最も古いものを捨てる(捨てられた保留の確定は「確認待ちのインポートがありません」で失敗する)。確認画面を開いたままWebViewが再読み込みされた場合(復号している最中の再読み込みを含む)は、識別子を持つ画面が無くなるため、ページの読み込みが始まった時点で、全ての保留が破棄され、復号している最中の結果も保留されない。ページ内の画面遷移は、ページの読み込みを起こさないため、この破棄は起きない(WebView2の場合。他のウェブビューでも同じとは限らない)。

貼り付けなどで前後に空白(全角スペースを含む)や、ゼロ幅文字(U+200B・U+200C・U+200D・U+2060)・BOM(U+FEFF)が混ざり、入力どおりでは復号できなかった場合に限り、それらを取り除いて1度だけ再試行する。入力どおりで復号できる場合は再試行しない(コマンドラインの`masker import`も同じ)。再試行で復号できたときは、確認画面の説明に、取り除いたことを示す一文が加わる(コマンドラインでは、標準エラー出力に、同じ趣旨が出る)。

## envインポート

`.env`ファイルのKEY=VALUEをそのまま取り込み、値をリテラル一致・固定値で置換するルールとして持つ新規プロファイルを作成する機能。正規表現ではなく実際の値そのものと一致させるため、値が変わらない限り確実に検出できる。

プロファイル管理画面の「envインポート」からファイルを選択すると、取り込み対象の選択画面が開く。値が短すぎるもの(4文字未満)、`PORT`・`DEBUG`等の非機微な運用系キー、引用符が閉じておらず複数行にまたがっている疑いのある値(PEM形式の秘密鍵等、1行ずつの解析では正しく取り込めないため)は既定で選択から外れるが、これはあくまで初期選択のヒントであり、最終的な取り込み対象はここで選び直せる。値は録画・配信時の映り込みを防ぐため既定で非表示(●)にしており、「値を表示」で一括切り替えできる。選択後、プロファイル名を入力すると作成される。

## 常駐トレイ

メイン画面を閉じる(×ボタン)とアプリは終了せずトレイに格納される。終了はトレイメニューの「終了」からのみ行う。

格納するとき、保留しているインポートの復号済みの内容を破棄し、パスフレーズ・復号済みの内容・.envの値を持つ画面(インポートのパスフレーズ入力・鍵ファイルの入力・確認、envインポートの選択・名前、編集中・保存先の選択中のエクスポート)を閉じる。書き込み中・書き出し済みのエクスポート画面は、閉じずに、パスフレーズの表示を伏せ字へ戻す(理由は、エクスポートの設計判断の記録)。

| メニュー項目 | 内容 |
|---|---|
| 開く | メインウィンドウを復帰させる(左クリックでも同じ動作) |
| クリップボードをマスク | アクティブプロファイルでクリップボードの内容をマスクし、そのまま書き戻す(アクティブプロファイル未設定の場合は無効) |
| プロファイル切り替え | サブメニューから直接アクティブプロファイルを切り替える |
| 自動起動 | OS起動時にアプリを自動起動するかどうかのトグル |
| 終了 | アプリを終了する |
